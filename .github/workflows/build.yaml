name: React basic demo

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Install node
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Install packages
        run: npm install
